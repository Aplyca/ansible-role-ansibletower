[primary]
localhost ansible_connection=local

[secondary]

[database]

[all:vars]
admin_password='{{ ansibletower.credentials.admin }}'
redis_password='{{ ansibletower.credentials.redis }}'

pg_host=''
pg_port=''

pg_database='awx'
pg_username='awx'
pg_password='{{ ansibletower.credentials.postgresql }}'

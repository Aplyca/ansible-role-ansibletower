---
ansibletower:
  no_log: False
  root: "/tmp"
  version: 3.2.2
  mode: install
  backup_file: tower-backup-latest.tar.gz
  backup_bucket: mybucket
  credentials:
    admin: "password"
    rabbitmq: ""
    postgresql: "password"
